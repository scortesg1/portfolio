import { Aleo, Inconsolata } from "next/font/google";

export const aleo = Aleo({
  subsets: ["latin"],
  weight: "variable",
});

export const inconsolata = Inconsolata({
  subsets: ["latin"],
  weight: "variable",
});